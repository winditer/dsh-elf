# dsh-elf i18n（跟随 DSH 语言，中/英）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** dsh-elf 界面与 host 错误消息跟随 DSH 设置的语言（zh/en）实时切换；系统提示词改中英双语模板，让模型按用户界面语言回复。

**Architecture:** 复用 DSH `@deepseek-ai/dsh-client-locale` 服务（与 dsh-market 相同模式）：client 注入 `'locale'`、注册 `dsh-elf` 命名空间双语词典、`useSyncExternalStore(locale.subscribe, locale.getSnapshot)` 零刷新切换；无 locale 服务时降级为 `document.documentElement.lang || navigator.language` 快照。host 侧错误消息按请求携带的 `lang` 参数选择语言，系统提示词输出双语模板。

**Tech Stack:** Node.js ≥22.19（ESM，`node:test`），React（无 JSX 语法，`React.createElement`），esbuild 打包（`__ModuleLoader__` wrapper）。

## Global Constraints

- 仓库：`/Users/haifeng/Documents/dsh-elf`；分支 `main`；git 身份 `winditer <winditer@users.noreply.github.com>`（已配置）。
- 命令均需 PATH 含 `/usr/local/bin`（node/npm 在此）：命令开头 `export PATH="/usr/local/bin:$PATH"`；npm 如遇 `~/.npm` 权限错误，追加 `--cache /tmp/npm-cache`。
- 每次变化的验证：`npm run check` 与 `npm test` 必须全绿；`npm run build` 必须成功；`dist/client.js` 是 gitignored 产物，**不提交**。
- 客户端只用普通浏览器 timer（无 cordis `timer` 服务）；bundle load id 必须等于包名 `dsh-elf`（已在 build.mjs 自动派生）。
- 语言判定约定（与 dsh-market 一致）：`String(active).toLowerCase().startsWith('zh') ? 'zh' : 'en'`；host 请求的 `lang` 只接受 `'zh' | 'en'`，缺省/非法为 `'zh'`（向后兼容）。
- 参数化文案统一 `{0}` 占位 + `String.prototype.replace('{0}', …)`（dsh-market 惯例）。
- host 的 400（无效 JSON）与 413（超限）错误由 `readJsonBody` 在解析前抛出，**无法得知 lang**，保持中文（文档化限制；正常客户端永不触达）。
- 提交粒度：每个 Task 结束一次提交；提交信息用惯例前缀（feat/test/docs/chore）。

---

### Task 1: 双语词典模块 `src/locales.js`（TDD）

**Files:**
- Create: `src/locales.js`
- Create: `test/locales.test.js`
- Modify: `package.json` 无

**Interfaces:**
- Produces: `export const NS = 'dsh-elf'`；`export const zh = {…}`；`export const en = {…}`（en 与 zh 键一一对应，`Record<keyof zh, string>`）。后续 Task 2/3 只消费这三个导出。

- [ ] **Step 1: 写失败测试**

创建 `test/locales.test.js`：

```js
import test from 'node:test'
import assert from 'node:assert/strict'
import { en, zh, NS } from '../src/locales.js'

test('namespace is "dsh-elf"', () => {
  assert.equal(NS, 'dsh-elf')
})

test('en mirrors zh key-for-key (1:1 dictionary)', () => {
  const zhKeys = Object.keys(zh).sort()
  const enKeys = Object.keys(en).sort()
  assert.deepEqual(enKeys, zhKeys)
})

test('core keys exist with both languages', () => {
  for (const k of ['name', 'title', 'follow', 'modelSource', 'empty', 'inputPh', 'hostFail', 'needCustomCfg']) {
    assert.ok(zh[k] && zh[k].length, `zh.${k} missing`)
    assert.ok(en[k] && en[k].length, `en.${k} missing`)
  }
  assert.ok(zh.hostFail.includes('{0}'), 'hostFail needs a {0} placeholder')
  assert.ok(en.hostFail.includes('{0}'))
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `export PATH="/usr/local/bin:$PATH" && cd /Users/haifeng/Documents/dsh-elf && npm test`
Expected: `Cannot find module '../src/locales.js'`（失败）。

- [ ] **Step 3: 实现词典**

创建 `src/locales.js`：

```js
// dsh-elf — zh/en dictionaries. zh is the source of truth; en mirrors it 1:1.

export const NS = 'dsh-elf';

export const zh = {
  name: 'DeepSeek 小精灵',
  title: 'DeepSeek 精灵',
  modelBadgeTitle: '当前模型',
  cfgBtn: '模型配置',
  mini: '最小化到小精灵',
  clearTitle: '清空聊天',
  clear: '清空',
  modelFollow: '跟随默认',
  modelSource: '模型来源',
  follow: '跟随会话默认',
  provider: '提供方',
  apiBase: 'API 地址',
  apiKey: 'API Key',
  model: '模型',
  reasoning: 'reasoning',
  modelPh: '例如 deepseek-chat',
  effortPh: '可选：high / medium / low',
  cfgHint: '自定义模式下小精灵直接调用该 API 地址（OpenAI 兼容 /chat/completions 流式接口）。取消勾选"跟随会话默认"后生效。',
  inputPh: '问小精灵点什么…',
  empty: '在小精灵里临时聊几句吧 ✨（不会写入会话记录）',
  copy: '复制',
  needCustomCfg: '请先填写 API 地址、API Key 和模型名',
  hostFail: '小精灵 Host 请求失败 ({0})',
  reqFailed: '请求失败',
  chatEnded: '对话结束',
  presetDeepseek: 'DeepSeek',
  presetOpenai: 'OpenAI',
  presetMoonshot: 'Moonshot Kimi',
  presetZhipu: '智谱 GLM',
  presetQwen: '通义千问',
  presetCustom: '自定义',
};

export const en = {
  name: 'DeepSeek Elf',
  title: 'DeepSeek Elf',
  modelBadgeTitle: 'Current model',
  cfgBtn: 'Model config',
  mini: 'Minimize to elf',
  clearTitle: 'Clear chat',
  clear: 'Clear',
  modelFollow: 'Following default',
  modelSource: 'Model source',
  follow: 'Follow session default',
  provider: 'Provider',
  apiBase: 'API base URL',
  apiKey: 'API Key',
  model: 'Model',
  reasoning: 'reasoning',
  modelPh: 'e.g. deepseek-chat',
  effortPh: 'Optional: high / medium / low',
  cfgHint: 'In custom mode the elf calls this API directly (an OpenAI-compatible /chat/completions streaming endpoint). Takes effect after unchecking "Follow session default".',
  inputPh: 'Ask the elf something…',
  empty: 'Have a quick chat with the elf ✨ (not saved to session history)',
  copy: 'Copy',
  needCustomCfg: 'Fill in the API base URL, API Key and model first',
  hostFail: 'Elf host request failed ({0})',
  reqFailed: 'Request failed',
  chatEnded: 'Chat ended',
  presetDeepseek: 'DeepSeek',
  presetOpenai: 'OpenAI',
  presetMoonshot: 'Moonshot Kimi',
  presetZhipu: 'Zhipu GLM',
  presetQwen: 'Qwen',
  presetCustom: 'Custom',
};
```

- [ ] **Step 4: 运行测试确认通过**

Run: `export PATH="/usr/local/bin:$PATH" && cd /Users/haifeng/Documents/dsh-elf && npm test`
Expected: 新增 3 个测试全部 PASS（连同原有 11 个共 14 个）。

- [ ] **Step 5: 提交**

```bash
git add src/locales.js test/locales.test.js
git commit -m "feat: add zh/en dictionaries for the elf UI"
```

---

### Task 2: 客户端接入 locale 服务并替换全部界面文案（TDD）

**Files:**
- Modify: `src/client.js`（全文——11 处文案、PRESETS、槽位注册、callHost）
- Modify: `test/client.test.js`

**Interfaces:**
- Consumes: Task 1 的 `NS`/`zh`/`en`。
- Produces: `export const inject = ['slots', 'locale']`；`apply(ctx)` 内 `locale` 变量（可能为 undefined）；ElfApp 内 `T`（当前语言词典）。

- [ ] **Step 1: 写失败测试**

在 `test/client.test.js` 末尾追加：

```js
test('client half follows the DSH locale service with zh/en dictionaries', () => {
  assert.match(client, /export const inject = \['slots', 'locale'\]/, 'client must inject the locale service')
  assert.match(client, /useSyncExternalStore\(/, 'locale changes must re-render via useSyncExternalStore')
  assert.match(client, /startsWith\('zh'\)/, 'zh must be detected by language prefix')
  assert.match(client, /navigator\.language/, 'must fall back to navigator.language when the locale service is absent')
  assert.match(client, /register\(NS, \{ zh, en \}\)/, 'dictionaries must be registered under the dsh-elf namespace')
})

test('client half no longer hardcodes user-visible Chinese literals', () => {
  for (const literal of ['问小精灵点什么', '跟随会话默认', '清空聊天', '请先填写 API 地址', '暂时聊几句']) {
    assert.doesNotMatch(client, new RegExp(literal), `literal must move to the dictionary: ${literal}`)
  }
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `export PATH="/usr/local/bin:$PATH" && cd /Users/haifeng/Documents/dsh-elf && npm test`
Expected: 新增 2 个测试 FAIL（现有 client 文件不含 `'locale'` 注入等）。

- [ ] **Step 3: 实现客户端改造**

在 `src/client.js` 顶部 import 区（`import React from 'react';` 之后）加入：

```js
import { en, zh, NS } from './locales.js';
```

将 `const plugin = { inject: ['slots'], apply(ctx) {` 改为：

```js
const plugin = {
  inject: ['slots', 'locale'],
  apply(ctx) {
    const locale = ctx.locale;
    const t = locale ? locale.bind(NS) : null;
    if (locale) {
      ctx.effect(() => locale.register(NS, { zh, en }), 'dsh-elf: dictionaries');
    }
    function currentLang() {
      const active = locale ? String(locale.getSnapshot().active) : String(document.documentElement.lang || navigator.language || 'zh');
      return active.toLowerCase().startsWith('zh') ? 'zh' : 'en';
    }
```

将 PRESETS 定义（原第 61-68 行）改为 label 走词典键：

```js
    const PRESETS = [
      { id: 'deepseek', labelKey: 'presetDeepseek', base: 'https://api.deepseek.com/v1' },
      { id: 'openai', labelKey: 'presetOpenai', base: 'https://api.openai.com/v1' },
      { id: 'moonshot', labelKey: 'presetMoonshot', base: 'https://api.moonshot.cn/v1' },
      { id: 'zhipu', labelKey: 'presetZhipu', base: 'https://open.bigmodel.cn/api/paas/v4' },
      { id: 'qwen', labelKey: 'presetQwen', base: 'https://dashscope.aliyuncs.com/compatible-mode/v1' },
      { id: 'custom', labelKey: 'presetCustom', base: '' },
    ];
```

将 `callHost` 的失败文案（原 `\`小精灵 Host 请求失败 (${response.status})\``）改为：

```js
    throw new Error((payload && payload.error) || zh.hostFail.replace('{0}', String(response.status)));
```

> 说明：callHost 在 apply 外（模块级），无 T；错误仅作兜底展示，用 zh 缺省即可（host 真实错误已按 lang 返回）。

在 `ElfApp()` 函数体顶部（`const [mode, setMode] = …` 之前）加入：

```js
      const lang = React.useSyncExternalStore(
        (cb) => (locale ? locale.subscribe(cb) : () => {}),
        () => currentLang(),
      );
      const T = lang === 'zh' ? zh : en;
```

> `React` 已在文件顶部 `import React from 'react'`；`useSyncExternalStore` 通过 `React.useSyncExternalStore` 使用。

按下面的对照表替换全部界面文案（`T.xxx` 为词典取值）：

| 原字面量（client.js 行号以当前文件为准） | 替换为 |
| --- | --- |
| `'…'`（effModel 初始值，第 234 行） | `'…'`（不变） |
| `'跟随默认'`（第 237 行） | `T.modelFollow` |
| `title: 'DeepSeek 小精灵'`（orb，第 581 行） | `title: T.name` |
| `'DeepSeek 精灵'`（窗口标题，第 589 行） | `T.title` |
| `title: '当前模型'`（第 590 行） | `title: T.modelBadgeTitle` |
| `title: '模型配置'`（第 592 行） | `title: T.cfgBtn` |
| `title: '最小化到小精灵'`（第 593 行） | `title: T.mini` |
| `title: '清空聊天'`、文本 `'清空'`（第 594 行） | `title: T.clearTitle` 与 `T.clear` |
| `'模型来源'`（第 603 行） | `T.modelSource` |
| `'跟随会话默认'`（第 610 行） | `T.follow` |
| `'提供方'`（第 614 行） | `T.provider` |
| `'API 地址'`（第 623 行） | `T.apiBase` |
| `'API Key'`（第 631 行） | `T.apiKey` |
| `'模型'`（第 639 行） | `T.model` |
| `'例如 deepseek-chat'`（第 641 行） | `T.modelPh` |
| `'reasoning'`（第 647 行） | `T.reasoning` |
| `'可选：high / medium / low'`（第 649 行） | `T.effortPh` |
| 自定义模式长提示（第 655 行整句） | `T.cfgHint` |
| `'请先填写 API 地址、API Key 和模型名'`（第 409 行） | `T.needCustomCfg` |
| `'请求失败'`（第 506 行） | `T.reqFailed` |
| `'对话结束'`（第 365 行） | `T.chatEnded` |
| `title: '复制'`（第 671 行） | `title: T.copy` |
| `'问小精灵点什么…'`（第 690 行） | `T.inputPh` |
| 空态 `'在小精灵里临时聊几句吧 ✨（不会写入会话记录）'`（第 676 行） | `T.empty` |
| 提供方下拉 `PRESETS.map((p) => … p.label)`（第 599-600 行） | `PRESETS.map((p) => React.createElement('option', { key: p.id, value: p.id }, T[p.labelKey]))` |

槽位注册（文件末尾 `ctx.slots.inject(...)`）改为：

```js
    ctx.slots.inject('shell.overlay', () => ctx.slots.register(
      { name: 'shell.overlay', id: 'dsh-elf', order: 500, label: () => (t ? t('name') : (currentLang() === 'zh' ? zh.name : en.name)), locale: NS },
      () => React.createElement(ElfApp, null),
    ));
```

> 若 `ctx.slots.register` 不接受未知元数据字段（`locale`），保留也不影响——dsh-market 同款用法；运行时按结构忽略未知字段。

- [ ] **Step 4: 运行测试确认通过 + 构建**

Run: `export PATH="/usr/local/bin:$PATH" && cd /Users/haifeng/Documents/dsh-elf && npm run check && npm test && npm run build`
Expected: check 通过；全部 16 个测试 PASS（11 原有 + 1 词典 + 2 client 新守卫 + 2 由 locales.test.js 带来的新测试，总数以实际为准，至少全绿）；build 成功输出 `dist/client.js`。

- [ ] **Step 5: 提交**

```bash
git add src/client.js test/client.test.js
git commit -m "feat: follow the DSH locale service and localize the elf UI (zh/en)"
```

---

### Task 3: host 错误消息按 lang 本地化 + 双语系统提示词（TDD）

**Files:**
- Modify: `src/host.js`（同时同步到 `lib/index.js`——两个文件内容必须一致）
- Modify: `test/host.test.js`

**Interfaces:**
- Consumes: client 请求体可选 `lang: 'zh' | 'en'`（Task 2 由 `elf.chat.start` 传入——见下方 Step 3 的实现，Task 2 的客户端仅开始传 lang 的代码在 Step 5 一并接入：在 `send()` 的 host 路由分支 `const payload = { messages: wire }` 改为 `const payload = { messages: wire, lang }`，`lang` 为 ElfApp 内 `lang === 'zh' ? 'zh' : 'en'`——未在 Task 2 完成则由本 Task 补上）。
- Produces: `elf.chat.start`/`elf.sessionModel`/`elf.chat.poll` 的错误文案按 `lang` 输出；`llm.stream(options).system` 为双语模板。

- [ ] **Step 1: 写失败测试**

在 `test/host.test.js` 末尾追加：

```js
test('chat.start errors follow the requested language', async () => {
  const handler = mount()
  const enRes = await call(handler, makeReq({ url: '/dsh-elf/api/elf.chat.start', json: { lang: 'en', messages: [] } }))
  assert.equal(enRes.status, 200)
  assert.deepEqual(parse(enRes).value, { ok: false, error: 'No message content' })
  const zhRes = await call(handler, makeReq({ url: '/dsh-elf/api/elf.chat.start', json: { messages: [] } }))
  assert.deepEqual(parse(zhRes).value, { ok: false, error: '没有消息内容' })
})

test('protocol errors follow the lang from the request body', async () => {
  const handler = mount()
  const badMethod = await call(handler, makeReq({ method: 'GET', url: '/dsh-elf/api/elf.sessionModel', json: { lang: 'en' } }))
  assert.equal(badMethod.status, 405)
  assert.deepEqual(parse(badMethod), { ok: false, error: 'Only POST is supported' })
  const unknown = await call(handler, makeReq({ url: '/dsh-elf/api/elf.nope', json: { lang: 'en' } }))
  assert.equal(unknown.status, 404)
  assert.deepEqual(parse(unknown), { ok: false, error: 'Unknown elf API' })
})

test('the system prompt is a bilingual template and follows lang', async () => {
  const captured = []
  const handler = mount({
    llmStream: async function* (options) { captured.push(options); },
  })
  await call(handler, makeReq({ url: '/dsh-elf/api/elf.chat.start', json: { lang: 'en', messages: [{ role: 'user', text: 'hi' }] } }))
  await call(handler, makeReq({ url: '/dsh-elf/api/elf.chat.start', json: { lang: 'zh', messages: [{ role: 'user', text: '嗨' }] } }))
  await new Promise((resolve) => setImmediate(resolve))
  assert.equal(captured.length, 2)
  assert.match(captured[0].system, /DeepSeek elf living in DSH/)
  assert.match(captured[0].system, /user's current UI language/)
  assert.match(captured[1].system, /住在 DSH 里的 DeepSeek 小精灵/)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `export PATH="/usr/local/bin:$PATH" && cd /Users/haifeng/Documents/dsh-elf && npm test`
Expected: 新增 3 个测试 FAIL（错误文案目前均为中文、无 lang 概念、提示词单一中文）。

- [ ] **Step 3: 实现 host 本地化**

把 `src/host.js` 顶部的常量区改为（在 `MAX_BODY_BYTES` 之后加入两张错误表与语言工具）：

```js
const MSG = {
  zh: {
    tooLarge: '请求内容过大',
    badJson: '请求不是有效 JSON',
    postOnly: '只支持 POST',
    unknown: '未知的小精灵 API',
    llmUnavailable: 'LLM 服务不可用',
    noMessages: '没有消息内容',
    noModel: '未配置模型',
    noChat: '对话不存在或已结束',
  },
  en: {
    tooLarge: 'Request body too large',
    badJson: 'Request body is not valid JSON',
    postOnly: 'Only POST is supported',
    unknown: 'Unknown elf API',
    llmUnavailable: 'LLM service unavailable',
    noMessages: 'No message content',
    noModel: 'No model configured',
    noChat: 'Chat not found or already ended',
  },
};

function langOf(args) {
  return args && (args.lang === 'en' || args.lang === 'zh') ? args.lang : 'zh';
}

function systemPrompt(lang) {
  const body = lang === 'en'
    ? 'You are the DeepSeek elf living in DSH. Be concise, friendly and direct, like a light-hearted assistant; this is a temporary chat, so do not assume you have tools or file access.'
    : '你是一只住在 DSH 里的 DeepSeek 小精灵。回答要简洁、友好、直接，像一位轻快的小助手；这是临时对话，不要假设你有工具或文件访问能力。';
  return body + ' 请使用用户当前界面语言回复（中文或英文）。 Reply in the user\'s current UI language (Chinese or English).';
}
```

改写 `readJsonBody` 内的两个 `throw new ElfHttpError(...)` 文案为 `MSG.zh.tooLarge` / `MSG.zh.badJson`（400/413 无法得知 lang，见 Global Constraints，保持中文常量引用以便集中维护）。

改写 `createApiRoute` 的 handler（方法检查前先读一次 body 取 lang；400/413 由 catch 直接响应）：

```js
function createApiRoute(handlers) {
  return async (req, res) => {
    let args = {};
    let lang = 'zh';
    try {
      args = await readJsonBody(req);
      lang = langOf(args);
    } catch (error) {
      const status = error instanceof ElfHttpError ? error.status : 500;
      writeJson(res, status, { ok: false, error: String((error && error.message) || error) });
      return;
    }
    const M = MSG[lang];
    if (req.method !== 'POST') {
      writeJson(res, 405, { ok: false, error: M.postOnly });
      return;
    }
    const pathname = new URL(req.url || '/', 'http://dsh.internal').pathname;
    const method = pathname.startsWith(API_PREFIX + '/')
      ? decodeURIComponent(pathname.slice(API_PREFIX.length + 1))
      : '';
    if (!method || method.includes('/')) {
      writeJson(res, 404, { ok: false, error: M.unknown });
      return;
    }
    const handler = handlers.get(method);
    if (!handler) {
      writeJson(res, 404, { ok: false, error: M.unknown + ': ' + method });
      return;
    }
    try {
      const value = await handler(args);
      writeJson(res, 200, { ok: true, value });
    } catch (error) {
      const status = error instanceof ElfHttpError ? error.status : 500;
      writeJson(res, status, { ok: false, error: String((error && error.message) || error) });
    }
  };
}
```

在 `apply(ctx)` 内：把 `SYSTEM_PROMPT` 常量定义删除，`elf.chat.start` handler 开头改为：

```js
    handlers.set('elf.chat.start', async (args) => {
      const llm = ctx.get('llm');
      const agentDefaultModel = ctx.get('agentDefaultModel');
      const lang = langOf(args);
      const M = MSG[lang];
      if (!llm) return { ok: false, error: M.llmUnavailable };
      const messages = Array.isArray(args && args.messages) ? args.messages : [];
      if (!messages.length) return { ok: false, error: M.noMessages };

      let provider = args && args.provider;
      let model = args && args.model;
      let reasoningEffort = args && args.reasoningEffort;
      if (!provider || !model) {
        if (!agentDefaultModel) return { ok: false, error: M.noModel };
        const sel = agentDefaultModel.currentSelection();
        provider = provider || sel.provider;
        model = model || sel.model;
        if (reasoningEffort === undefined) reasoningEffort = sel.reasoningEffort;
      }
      if (!provider || !model) return { ok: false, error: M.noModel };

      const chatId = 'elf-chat-' + Date.now() + '-' + (seq++);
      const entry = { text: '', done: false, error: null };
      chats.set(chatId, entry);

      const wire = messages.map(buildMessage);
      const options = { provider, model, messages: wire, system: systemPrompt(lang) };
      if (reasoningEffort) options.reasoningEffort = reasoningEffort;
      // …原本的流式 IIFE 不变…
```

`elf.chat.poll` 的"对话不存在或已结束"改为 `MSG[langOf(args)].noChat`。`apply` 内删除原 `SYSTEM_PROMPT` 定义（原第 76 行）。

**同步**：把 `src/host.js` 的完整新内容覆盖复制到 `lib/index.js`（`cp src/host.js lib/index.js`），两者必须逐字节一致。

- [ ] **Step 4: 客户端开始传 lang**

查看 `src/client.js` 的 `send()` 中 host 路由分支：把

```js
        const payload = { messages: wire };
```

改为

```js
        const payload = { messages: wire, lang: lang === 'zh' ? 'zh' : 'en' };
```

（`lang` 即 ElfApp 内 `useSyncExternalStore` 得到的语言。）

- [ ] **Step 5: 运行测试确认通过**

Run: `export PATH="/usr/local/bin:$PATH" && cd /Users/haifeng/Documents/dsh-elf && npm run check && npm test`
Expected: 全部测试 PASS（新增 3 个 host 用例 + 既有 13 个）。`diff lib/index.js src/host.js` 输出为空。

- [ ] **Step 6: 提交**

```bash
git add src/host.js lib/index.js test/host.test.js src/client.js
git commit -m "feat: localize host errors by request lang and use a bilingual system prompt"
```

---

### Task 4: 版本 2.2.0 + CHANGELOG + README 双语说明（无测试）

**Files:**
- Modify: `package.json`（version `2.1.0` → `2.2.0`）
- Modify: `CHANGELOG.md`
- Modify: `README.md`、`README.zh.md`

- [ ] **Step 1: package.json 版本号**

把 `package.json` 第 3 行 `"version": "2.1.0"` 改为 `"version": "2.2.0"`。

- [ ] **Step 2: CHANGELOG 顶部插入 2.2.0 条目**

```markdown
## 2.2.0 — 2026-08-19

- UI now follows the DSH settings language and switches between Chinese and
  English live (no reload needed); falls back to the page language on hosts
  without the locale service.
- Host error messages follow the request language; the system prompt is a
  bilingual template so the elf replies in the user's current UI language.
```

- [ ] **Step 3: README 双语加 Language 说明**

`README.md` 的 Features 列表末尾加一条：

```markdown
- **Follows your language** — the elf UI switches between 中文 / English automatically, matching the language setting in DSH; no reload needed
```

`README.zh.md` 对应位置加：

```markdown
- **跟随语言** — 界面随 DSH 设置的语言自动在中英文之间切换，无需刷新页面
```

并把两个 README 中 `minimumReleaseAgeExclude` 示例（`dsh-elf@2.1.0`）改为 `dsh-elf@2.2.0`（README.md 第 ~158 行；README.zh.md 相同文案处）。

- [ ] **Step 4: 验证**

Run: `export PATH="/usr/local/bin:$PATH" && cd /Users/haifeng/Documents/dsh-elf && npm run check && npm test && npm run build && git diff --stat`
Expected: 全绿；`dist/client.js` 生成；diff 只含 package.json / CHANGELOG.md / README.md / README.zh.md。

- [ ] **Step 5: 提交**

```bash
git add package.json CHANGELOG.md README.md README.zh.md
git commit -m "chore: bump to 2.2.0 — i18n release"
```

---

### Task 5: 全量验证与推送

**Files:** 无代码改动（验证 + 推送）。

- [ ] **Step 1: 完整验证**

Run: `export PATH="/usr/local/bin:$PATH" && cd /Users/haifeng/Documents/dsh-elf && npm run check && npm test && npm run build`
Expected: check 通过；测试全绿；构建成功。

- [ ] **Step 2: 确认产物与历史**

Run: `git log --oneline -8 && git status --short`
Expected: 顶部为 i18n 相关 4-5 笔提交；`dist/client.js` 未被跟踪；工作区干净（.pr-pack 已被 gitignore）。

- [ ] **Step 3: 推送**

Run: `cd /Users/haifeng/Documents/dsh-elf && git push origin main`
Expected: 成功快进（凭据为 osxkeychain 中的 winditer PAT）。

- [ ] **Step 4: 重触发 PR gate（条件：当前时间 ≥ 2026-08-19 11:21:21 UTC）**

若已过阈值，在 `/tmp/awesome-dsh-plugin`（或重新 clone fork）执行：

```bash
cd /tmp/awesome-dsh-plugin
git config credential.helper osxkeychain
git fetch https://github.com/winditer/awesome-dsh-plugin.git add/dsh-elf
git checkout -B add/dsh-elf FETCH_HEAD
git commit --allow-empty -m "retrigger: dsh-elf repo passed the 1-day age threshold"
git push https://github.com/winditer/awesome-dsh-plugin.git add/dsh-elf:add/dsh-elf
```

并在 GitHub 上确认 PR #1694 的 `Submission gate` 变为 success（API：
`curl -s https://api.github.com/repos/awesome-dsh-plugin/awesome-dsh-plugin/commits/<新sha>/check-runs`）。
若未到阈值：记录当前时间与预计可重触发时间，留给下一轮。

- [ ] **Step 5: （可选）npm 发布 2.2.0**

先探测：`export PATH="/usr/local/bin:$PATH" && npm whoami --cache /tmp/npm-cache`。
若已登录：`npm publish --cache /tmp/npm-cache`（发布后 README 的 minimumReleaseAge 示例已同步）。
若未登录：告知用户自行发布，并说明 `dsh plugin add dsh-elf` 需等发布满 24h 或加入
`minimumReleaseAgeExclude`（README 已说明）。